import { View, Text } from "react-native";
import React from "react";
import { ICardDetailProps } from "@/app/(root)/card/detail/[id]";

const LinkDetail = ({ card }: ICardDetailProps) => {
  return (
    <View>
      <Text>LinkDetail</Text>
    </View>
  );
};
export default LinkDetail;
